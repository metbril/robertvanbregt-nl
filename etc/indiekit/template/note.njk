---
slug: '{{ published | date('X') }}'
date: '{{ published }}'
{%- if category %}
tags:
{%- for c in category %}
  - {{ c }}
{%- endfor %}
{%- endif %}
mf2:
{%- if syndicateTo %}
  syndication:
{%- for s in syndicateTo %}
    - {{ s }}
{%- endfor %}
{%- endif %}
---
{{ content | safe }}