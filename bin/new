#!/usr/bin/env bash

contentdir="content"

section=$1
shift
# slug=$@
slug=`echo $@ | sed -e 's/[^[:alnum:]]/-/g' | tr A-Z a-z | tr -s '-'`
path=`date +%Y/%m`

file="$section/$path/$slug/index.md"
hugo new $file
code $contentdir/$file